import 'package:flutter/material.dart';

class FavoriteRepos extends StatelessWidget {
  const FavoriteRepos({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: Colors.amber),
    );
  }
}
